class Solution {
public:
    int countCommas(int n) {
        int count {0};
        int factor {0};

        while (n >= 1000)
        {
            factor = n / 1000;
            n %= 1000;
            count += (n % 1000) + ((1000 * factor) - 1000) + 1;
        }

        return count;
    }
};
